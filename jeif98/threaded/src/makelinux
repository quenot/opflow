
              ASY = ../linux

              BIN = $(ASY)/bin
              OBD = $(ASY)/obj

               RM = rm -f
               CC = gcc
           CFLAGS = -Dlinux -O2 -Wall -fopenmp -pthread

include common.mk
