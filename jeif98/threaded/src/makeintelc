
              ASY = ../intelc

              BIN = $(ASY)/bin
              OBD = $(ASY)/obj

               RM = rm -f
               CC = icc
           CFLAGS = -Dlinux -O2 -qopenmp -pthread -fp-model consistent -Wall

include common.mk
